  jobs:
    linting:
      runs-on: ubuntu-latest

      steps:
        - uses: actions/checkout@v3

        - name: Setup python
          uses: actions/setup-python@v3
          with:
            python-version: 3.8

        - name: pre-commit
          run: |
            pip install pre-commit
            pre-commit run -a